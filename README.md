# synkey-test-library

A SynKey remote library: `.synkey` archives + `manifest.json`, generated by
[synkey-lib](https://github.com/aronjanosch/synkey-lib).

Add this manifest URL in the SynKey app:

```
https://raw.githubusercontent.com/aronjanosch/synkey-test-library/master/manifest.json
```
